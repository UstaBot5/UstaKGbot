import telebot

TOKEN = "7901632898:AAFF5UShYE8-cEC71ABnJX9gC_zpClwBtZQ"
ADMIN_USERNAME = "@alfaperson42"

bot = telebot.TeleBot(TOKEN)

@bot.message_handler(commands=['start'])
def start_message(message):
    bot.send_message(message.chat.id, "Здравствуйте! Напишите, какую услугу вы ищете, и мы вам подберём мастера.")

@bot.message_handler(func=lambda message: True)
def forward_request(message):
    # Перешлём сообщение админу
    bot.send_message(message.chat.id, "Спасибо! Ваша заявка отправлена.")
    bot.send_message(ADMIN_USERNAME, f"Заявка от @{message.from_user.username or message.from_user.first_name}:\n{message.text}")

bot.polling()
